const altura_cm = 185;

const altura_m = 1.85;

const peso_kg = 82.54;

const redondear_arriba = Math.round(altura_m);

const redondear_abajo = Math.floor(peso_kg);

var max_valor;

if(Number.MAX_VALUE+1 === Number.MAX_VALUE) {
    max_valor = true;
}else {
    max_valor = false;
}



